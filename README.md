Running `main.go` file:
* Docker
    * To run `main.go` via docker use:
        * `docker build -t go-app . && docker run -it go-app`
* Without Docker (you have to have golang installed)
    * To run `main.go`:
        * `go run main.go` or `go build main.go && ./main.go`

